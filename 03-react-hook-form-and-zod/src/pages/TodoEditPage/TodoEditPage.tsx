import { TodoEditTemplate } from "../../components/templates/TodoEditTemplate";

/**
 * Todo編集ページ
 * URLパラメータからIDを取得し、該当するTodoの編集フォームを表示
 */
export const TodoEditPage = () => {
  return <TodoEditTemplate />;
};
